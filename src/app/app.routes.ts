import { Routes } from '@angular/router';
import { SimulationComponent } from './simulation/simuation.component';

export const routes: Routes = [
  {
    path: '',
    component: SimulationComponent,
  },
];
